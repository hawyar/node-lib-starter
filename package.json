{
  "name": "node-esm-starter",
  "version": "1.0.0",
  "description": "My Node.js starter",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "tap --no-coverage --reporter=specy -J --color",
    "build:docs": "rm -rf docs && jsdoc index.js -t ./node_modules/foodoc/template -d docs -r -e utf8 --readme README.md -c conf.json",
    "serve:docs": " rm -rf docs && npm run build:docs && node static",
    "build:node": "esbuild index.js --format=cjs --bundle --platform=node --outfile=build/index.min.js --minify",
    "build:exec": "pkg index.js -o build/index-exec",
    "build": "npm run build:node && npm run build:exec"
  },
  "author": "hawyar",
  "devDependencies": {
    "esbuild": "^0.13.4",
    "finalhandler": "^1.1.2",
    "foodoc": "^0.0.9",
    "jsdoc": "^3.6.7",
    "pkg": "^5.3.1",
    "tap": "^15.0.9"
  },
  "license": "MIT"
}
